import math

n_sin=float(input('number to take sin of: '))
p=math.sin(n_sin)
print(p)
n_log=float(input('number to take log of: '))
q=math.log(n_log)
print(q)
n_sqrt=float(input('number to take square root of: '))
r=math.sqrt(n_sqrt)
print(r)
